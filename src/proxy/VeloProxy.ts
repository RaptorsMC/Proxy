// this is bound to change.
class VeloProxy {
     /**
      * start
      */
     public start() {
          // we can't do this yet because deno doesn't have any libs for udp4 sockets
     }

     public send(stream: BinaryStream): void {
          
     }
}